# SpecDist requirements
# PyTorch is installed separately by setup_env.py (CUDA-version-specific wheel).
# Install this file AFTER torch:
#   pip install -r requirements.txt

# Core ML
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.29.0
datasets>=2.18.0
tokenizers>=0.19.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Training utilities
bitsandbytes>=0.43.0
scipy>=1.12.0

# Numerics
numpy>=1.26.0
pandas>=2.2.0

# Web dashboard
flask>=3.0.0
flask-cors>=4.0.0

# Utilities
tqdm>=4.66.0
requests>=2.31.0
huggingface-hub>=0.22.0
pyyaml>=6.0                   # YAML config loading (orchestration/configs/*.yaml)
wandb>=0.16.0                  # Experiment tracking + hyperparameter sweeps

# Original OSD deps (kept for compatibility)
fschat==0.2.23
packaging
